dec media : num # num # num -> num;
dec somaMoedas : num # num # num # num # num -> num;
dec volumeEsfera : num -> num;
dec ultimoDigito : num -> num;

--- media (x, y, z) <= (x + y + z) / 3;

--- somaMoedas (a, b, c, d, e) <= a + b + c + d + e;

--- volumeEsfera r <= 4 / 3 * pi * pow (r, 3);

--- ultimoDigito x <= x mod 10;
